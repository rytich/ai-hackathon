# Project Agent Skills

Store repeatable agent workflows here.

Each skill should live in `.agents/skills/<name>/SKILL.md` and include:

- `name`
- `description`
- `argument-hint`
- `allowed-tools`
- workflow steps
- safety rules
- references

Tool-specific compatibility files should point back to this directory instead of duplicating detailed instructions.
